export default function MinimalPropertyPage() {
  return (
    <div style={{ padding: '40px' }}>
      <h1>Minimal Property Page</h1>
      <p>This page has no imports except the default React included by Next.js</p>
      <p>If this page works, the issue is with imports.</p>
      <p>If this page also fails, the issue is with the route itself.</p>
    </div>
  );
}